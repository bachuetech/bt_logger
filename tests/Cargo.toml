[package]
name = "BTLogger"
version = "0.2.0"
edition = "2021"
publish = false

[lints.rust]
unexpected_cfgs = { level = "allow", check-cfg = ['cfg(lib_build)'] }

[dependencies.log]
path = ".."

[dev-dependencies.rustversion]
version = "1.0"